<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.islamy">

    <application
        android:allowBackup="true"
        android:icon="@drawable/quralogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/quralogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Islamy.NoActionBar">
        <activity
            android:name=".ui.suraDetails.SuraDetails"
            android:label="@string/title_activity_sura_details"
            android:theme="@style/Theme.Islamy.NoActionBar"></activity>
        <activity
            android:name=".ui.homee.Home"
            android:exported="true" />
        <activity
            android:name=".ui.Splash.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>